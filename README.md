**CARA PAKAI**
1. simpan script
2. di dalam script, ubah domain sesuai domain kamu
3. beri izin. jalankan: sudo chmod +x setup-subdomain.sh
4. jalankan script nya. formatnya: bash setup-subdomain.sh (sub-domain-kamu) (tempat-folder). contohnya: bash setup-subdomain.sh portofolio /var/www/portofolio
